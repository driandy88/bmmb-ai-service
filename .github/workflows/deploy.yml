name: Test, Build & Deploy — Extraction

on:
  pull_request:
    branches: [main]
    paths: ["services/extraction/**", "libs/bmmb_common/**"]
  push:
    branches: [main]
    paths: ["services/extraction/**", "libs/bmmb_common/**"]

env:
  PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
  REGION: asia-southeast1
  SERVICE_NAME: extraction-service
  REPO_NAME: bmmb-ai-service             # shared Artifact Registry repo across all services in this monorepo
  WORKDIR: services/extraction

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write   # required for keyless auth if you switch to Workload Identity later
    steps:
      - uses: actions/checkout@v4

      - id: auth
        name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          # tests hit the real docs_extractor_dev BigQuery dataset (see
          # tests/test_schema_builder.py) — same credentials as the deploy
          # job below, just scoped to reads against dev, never prod.
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        working-directory: ${{ env.WORKDIR }}
        run: pip install -r requirements.txt -r requirements-dev.txt

      - name: Run tests
        working-directory: ${{ env.WORKDIR }}
        env:
          APP_ENV: dev
          GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
        run: pytest tests/ -v

  # Only runs after tests pass, and only on pushes to main (not on PRs)
  deploy:
    needs: test
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write   # required for keyless auth if you switch to Workload Identity later
    steps:
      - uses: actions/checkout@v4

      - id: auth
        name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          # Simplest path: a Service Account JSON key stored as a GitHub secret.
          # See README.md "CI/CD setup" for the one-time gcloud commands, and
          # for the Workload Identity Federation alternative (no long-lived key).
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker for Artifact Registry
        run: gcloud auth configure-docker ${{ env.REGION }}-docker.pkg.dev --quiet

      - name: Build image
        run: |
          docker build -t ${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPO_NAME }}/${{ env.SERVICE_NAME }}:${{ github.sha }} ${{ env.WORKDIR }}

      - name: Push image
        run: |
          docker push ${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPO_NAME }}/${{ env.SERVICE_NAME }}:${{ github.sha }}

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy ${{ env.SERVICE_NAME }} \
            --image=${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPO_NAME }}/${{ env.SERVICE_NAME }}:${{ github.sha }} \
            --region=${{ env.REGION }} \
            --platform=managed \
            --allow-unauthenticated \
            --service-account=extraction-service-sa@${{ env.PROJECT_ID }}.iam.gserviceaccount.com \
            --set-env-vars=ALLOWED_ORIGINS=${{ secrets.ALLOWED_ORIGINS }},GCP_PROJECT_ID=${{ env.PROJECT_ID }},APP_ENV=prod

      - name: Show deployed URL
        run: gcloud run services describe ${{ env.SERVICE_NAME }} --region=${{ env.REGION }} --format="value(status.url)"