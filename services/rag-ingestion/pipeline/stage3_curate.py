"""
Stage 3 · Curate (brief §5) — PLACEHOLDER, implemented in Phase 3.

Reorganise page-ordered Markdown into one canonical document per program with
consistent sections (Overview · Eligibility · Financing size · Purpose · Tenure ·
Fees & benefits · Required documents). Route pages across corpora (program /
sales_dir), drop filler, and CLASSIFY access_tier here (internal criteria pages
-> internal, §11). Writes data/03_curated/<corpus>/<program_code>.md with YAML
front-matter. REFUSES to run without a Stage-2 signoff.
"""
from __future__ import annotations


def run(args) -> int:
    print("[stage3_curate] scaffold only — implemented in Phase 3. See README.md build order.")
    return 0
