"""
Stage 2 · Verify (brief §5) — human gate. PLACEHOLDER, implemented in Phase 2.

Build a review artifact pairing each page image side by side with its extracted
Markdown, numeric values highlighted. A product owner signs off; record
approved_by / approved_at / doc_version into data/02_verified/<doc_id>/signoff.json.
Stage 3 must REFUSE to run without a valid signoff — a hard gate, not a warning.
"""
from __future__ import annotations


def run(args) -> int:
    print("[stage2_verify] scaffold only — implemented in Phase 2. See README.md build order.")
    return 0
