-r requirements.txt

# Test + notebook tooling.
pytest==8.*
httpx==0.27.*          # FastAPI TestClient (notebook Part B, tests/test_api)
jupyter==1.*
openpyxl==3.*          # read the Customer Service workbook in the notebook
