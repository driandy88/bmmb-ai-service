-r requirements.txt

# Test + notebook tooling.
pytest==8.*
httpx==0.28.1           # FastAPI TestClient (notebook Part B, tests/test_api); pinned exact --
                        # google-genai==1.47.0 requires httpx>=0.28.1 (see bbox_generator/validation)
jupyter==1.*
openpyxl==3.*          # read the Customer Service workbook in the notebook
