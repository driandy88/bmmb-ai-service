# Runtime deps. Pinned by major to match the other services in this repo
# (see services/extraction/requirements.txt). google-genai is imported lazily
# and only needed when LLM_BACKEND=vertex; the default stub path needs none of
# the GCP packages.
fastapi[standard]==0.115.*
uvicorn[standard]==0.32.*
pydantic==2.*
python-dotenv==1.0.*
PyYAML==6.*

# Orchestration (brief §3 — LangGraph state machine).
langgraph==0.2.*
langchain-core==0.3.*

# Vertex AI (Gemini) — used only by LLM_BACKEND=vertex.
google-genai==0.3.*

# Cloud SQL — the Postgres checkpointer / audit durability layer
# (SESSION_STORE_BACKEND=postgres, AUDIT_BACKEND=cloudsql) and the RAG vector
# store in the `bmmb` database (RAG_BACKEND=pgvector).
cloud-sql-python-connector[pg8000]==1.*
SQLAlchemy==2.*

# RAG document parsing (agents/rag/ingestion/parser.py). Imported lazily, per
# format — a Markdown-only corpus touches neither.
pypdf==6.*
python-docx==1.*

# RAG embeddings, RAG_EMBEDDING_PROVIDER=hf only (Inference API client — no
# torch, no local weights). Imported lazily; the Vertex provider never needs it.
huggingface-hub==1.*
