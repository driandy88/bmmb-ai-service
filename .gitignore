# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# Env
.env

# Notebook checkpoints
.ipynb_checkpoints/

# OS
.DS_Store

# Your own test documents -- never commit real/sensitive documents.
# Only this folder's placeholder README stays tracked.
sample_docs/private/*
!sample_docs/private/README.md
# The extraction service moved under services/extraction/ (see README restructuring
# note), so its private test docs must be ignored under the nested path too.
services/extraction/sample_docs/private/